<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.bookkeeping.modules.bkeeping.mapper.ClassifyMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hc.bookkeeping.modules.bkeeping.entity.Classify">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="pid" property="pid" />
        <result column="user_id" property="userId" />
        <result column="image" property="image" />
        <result column="sort" property="sort" />
        <result column="type" property="type" />
        <result column="enable" property="enable" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, pid, user_id, image, sort, type, enable
    </sql>

    <!-- 批量插入用户分类 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO classify (name, pid, user_id, image, sort, type, enable)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.pid}, #{item.userId}, #{item.image}, #{item.sort}, #{item.type}, #{item.enable})
        </foreach>
    </insert>

</mapper>
