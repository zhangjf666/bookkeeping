<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hc.bookkeeping.modules.bkeeping.mapper.UserTagMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hc.bookkeeping.modules.bkeeping.entity.UserTag">
        <id column="id" property="id" />
        <result column="code" property="code" />
        <result column="user_id" property="userId" />
        <result column="name" property="name" />
        <result column="color" property="color" />
        <result column="pinned" property="pinned" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, code, user_id, name, color, pinned, create_time, update_time
    </sql>

    <!-- 插入用户标签 -->
    <insert id="insert" parameterType="com.hc.bookkeeping.modules.bkeeping.entity.UserTag">
        INSERT INTO user_tag (code, user_id, name, color, pinned, create_time, update_time)
        SELECT IFNULL(MAX(code), 1000) + 1, #{userId}, #{name}, #{color}, #{pinned}, #{createTime}, #{updateTime} FROM user_tag WHERE user_id = #{userId};
    </insert>

</mapper>
